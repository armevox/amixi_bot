services:
  - type: web
    name: telegram-gemini-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_bot_gemini.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
    plan: free
